# Changelog

## latest

- Rename app to GitHub Actions Runner.
- Set required options to `github_url` and `runner_token`.
- Keep `runner_name`, `runner_labels`, and `runner_workdir` optional with defaults.
- Align app structure with Home Assistant Apps documentation.
